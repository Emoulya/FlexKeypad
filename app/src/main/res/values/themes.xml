<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme after splash finishes -->
    <style name="Theme.FlexKeypad" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="android:statusBarColor">@color/splash_background</item>
        <item name="android:navigationBarColor">@color/splash_background</item>
    </style>

    <!-- Starting Splash Screen theme (Android 12+ API compatible via core-splashscreen) -->
    <style name="Theme.FlexKeypad.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.FlexKeypad</item>
    </style>

</resources>